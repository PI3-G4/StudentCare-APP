import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:studentcare/app/../Components/survey/survey_widget.dart';

void main() {
//  testWidgets('SurveyWidget has message', (tester) async {
//    await tester.pumpWidget(buildTestableWidget(SurveyWidget()));
//    final textFinder = find.text('Survey');
//    expect(textFinder, findsOneWidget);
//  });
}
