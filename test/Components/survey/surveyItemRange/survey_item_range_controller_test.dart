import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:studentcare/app/../Components/survey/surveyItemRange/survey_item_range_controller.dart';
import 'package:studentcare/app/app_module.dart';

void main() {
  initModule(AppModule());
  // SurveyItemRangeController surveyitemrange;
  //
  setUp(() {
    //     surveyitemrange = AppModule.to.get<SurveyItemRangeController>();
  });

  group('SurveyItemRangeController Test', () {
    //   test("First Test", () {
    //     expect(surveyitemrange, isInstanceOf<SurveyItemRangeController>());
    //   });

    //   test("Set Value", () {
    //     expect(surveyitemrange.value, equals(0));
    //     surveyitemrange.increment();
    //     expect(surveyitemrange.value, equals(1));
    //   });
  });
}
