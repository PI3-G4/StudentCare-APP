import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:studentcare/app/../Components/survey/surveyItemStar/survey_item_star_controller.dart';
import 'package:studentcare/app/app_module.dart';

void main() {
  initModule(AppModule());
  // SurveyItemStarController surveyitemstar;
  //
  setUp(() {
    //     surveyitemstar = AppModule.to.get<SurveyItemStarController>();
  });

  group('SurveyItemStarController Test', () {
    //   test("First Test", () {
    //     expect(surveyitemstar, isInstanceOf<SurveyItemStarController>());
    //   });

    //   test("Set Value", () {
    //     expect(surveyitemstar.value, equals(0));
    //     surveyitemstar.increment();
    //     expect(surveyitemstar.value, equals(1));
    //   });
  });
}
