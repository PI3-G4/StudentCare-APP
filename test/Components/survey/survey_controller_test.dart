import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:studentcare/app/../Components/survey/survey_controller.dart';
import 'package:studentcare/app/app_module.dart';

void main() {
  initModule(AppModule());
  // SurveyController survey;
  //
  setUp(() {
    //     survey = AppModule.to.get<SurveyController>();
  });

  group('SurveyController Test', () {
    //   test("First Test", () {
    //     expect(survey, isInstanceOf<SurveyController>());
    //   });

    //   test("Set Value", () {
    //     expect(survey.value, equals(0));
    //     survey.increment();
    //     expect(survey.value, equals(1));
    //   });
  });
}
