import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:studentcare/app/modules/institution/institution_controller.dart';
import 'package:studentcare/app/modules/institution/institution_module.dart';

void main() {
  initModule(InstitutionModule());
  // InstitutionController institution;
  //
  setUp(() {
    //     institution = InstitutionModule.to.get<InstitutionController>();
  });

  group('InstitutionController Test', () {
    //   test("First Test", () {
    //     expect(institution, isInstanceOf<InstitutionController>());
    //   });

    //   test("Set Value", () {
    //     expect(institution.value, equals(0));
    //     institution.increment();
    //     expect(institution.value, equals(1));
    //   });
  });
}
