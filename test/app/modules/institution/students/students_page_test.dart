import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:studentcare/app/modules/institution/students/students_page.dart';

void main() {
  testWidgets('StudentsPage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(StudentsPage(title: 'Students')));
    //  final titleFinder = find.text('Students');
    //  expect(titleFinder, findsOneWidget);
  });
}
