import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:studentcare/app/modules/institution/institution_page.dart';

void main() {
  testWidgets('InstitutionPage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(InstitutionPage(title: 'Institution')));
    //  final titleFinder = find.text('Institution');
    //  expect(titleFinder, findsOneWidget);
  });
}
